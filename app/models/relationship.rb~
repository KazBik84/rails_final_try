class Relationship < ActiveRecord::Base
	# Ponieważ nie istnieje model "follower" lub "followed" musimy podać nazwę klasy modelu do którego 
	#  przypiszemy zależność. 
	belongs_to :follower, class_name: "User"
	belongs_to :followed, class_name: "User"
end
